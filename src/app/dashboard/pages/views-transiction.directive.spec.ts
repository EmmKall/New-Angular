/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ViewsTransictionDirective } from './views-transiction.directive';

describe('Directive: ViewsTransiction', () => {
  it('should create an instance', () => {
    const directive = new ViewsTransictionDirective();
    expect(directive).toBeTruthy();
  });
});
